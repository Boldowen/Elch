# Install manually in an isolated environment. The application never installs or
# imports these dependencies during normal startup.
torch>=2.4,<3
transformers>=4.48,<5
datasets>=3,<5
peft>=0.14,<1
accelerate>=1,<2
bitsandbytes>=0.45,<1; platform_system == "Linux"
safetensors>=0.5,<1
